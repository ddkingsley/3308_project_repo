from app import db

#basic user table using flask-sqlalchemy
class User(db.Model):
    id = db.Column(db.Integer, primary_key=True)
    username = db.Column(db.String(64), index=True, unique=True)
    password_hash = db.Column(db.String(128))
    zodiac = db.Column(db.String(64))